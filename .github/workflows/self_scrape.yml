name: Self Scrape Prices

concurrency:
  group: scrape-prices
  cancel-in-progress: false

permissions:
  contents: read

on:
  push:
    branches:
      - main
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: # allow manual run

jobs:
  scrape:
    runs-on: self-hosted

    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install deps
        shell: cmd
        run: npm ci --include=optional

      - name: Ensure sharp linux-x64 runtime
        if: runner.os == 'Linux'
        shell: cmd
        run: |
          npm install --no-save --include=optional --os=linux --cpu=x64 sharp
          node -e "import('sharp').then(() => console.log('sharp ok on linux-x64'))"

      - name: Ensure sharp win32-x64 runtime
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          npm install --no-save --include=optional --os=win32 --cpu=x64 sharp
          node -e "import('sharp').then(() => console.log('sharp ok on win32-x64'))"

      - name: Install Playwright browsers (Linux)
        if: runner.os == 'Linux'
        shell: cmd
        run: npx playwright install --with-deps

      - name: Install Playwright browsers (non-Linux)
        if: runner.os != 'Linux'
        shell: cmd
        run: npx playwright install

      - name: Run scraper
        shell: cmd
        run: node scrape.js
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
